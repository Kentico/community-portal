@using Kentico.Community.Portal.Web.Features.Integrations

@model TemplateViewModel<IntegrationsLandingPageTemplateProperties>

@{
    var templateModel = Model.GetTemplateModel<IntegrationsLandingPage>();
}

<vc:page-heading page="templateModel" />

<main>
    <vc:integrations-list />

    <editable-area area-identifier="top" allow-widget-output-cache="View.CacheEnabled"
        widget-output-cache-expires-after="TimeSpan.FromMinutes(5)" />
</main>